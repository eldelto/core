{{define "content"}} 

<ul>
  {{range .Entries}}
  <li>
    {{if .IsDir}}
    <a href="{{$.CurrentURL.JoinPath .Name}}">{{.Name}}</a>
    {{else}}
    <a href="{{$.CurrentURL.JoinPath .Name}}?download=true">{{.Name}}</a>
    {{end}}
  </li>
  {{end}}
</ul>

{{end}}
